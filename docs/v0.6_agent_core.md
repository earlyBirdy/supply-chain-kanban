# v0.6 Agent Core

## Adds (requested)
- ERP/MES/WMS ingest adapters (CSV drop folders)
- Data quality gates (BLOCK/WARN with persistence)
- Scenario outputs persisted per case (`agent_scenarios`)

## Run demo
```bash
make demo
make logs
make psql
```

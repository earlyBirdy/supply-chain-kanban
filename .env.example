# Copy to .env:
#   cp .env.example .env

# -------- Postgres (demo) --------
POSTGRES_USER=demo
POSTGRES_PASSWORD=demo
POSTGRES_DB=demo

# -------- App / API --------
APP_ENV=dev
DEV_MODE=1

# SQLAlchemy URL used by api/agent/cleanup containers
AGENT_DB_URL=postgresql+psycopg2://demo:demo@db:5432/demo

# Agent loop
POLL_SECONDS=60
RISK_CREATE_THRESHOLD=70
ALERT_THRESHOLD=85

# -------- Governance (policy.yaml) --------
# Optional: store policy in a mounted volume path
# GOV_POLICY_PATH=/data/policy.yaml

# -------- Idempotency TTL / cleanup --------
IDEMPOTENCY_TTL_HOURS=24
IDEMPOTENCY_CLEANUP_INTERVAL=3600

# -------- JWT / SSO (prefer API gateway verification + X-User-Role headers) --------
JWT_VERIFY=0
JWT_SECRET=
JWT_ALG=HS256
JWT_ROLE_CLAIM=role
JWT_EMAIL_CLAIM=email

# -------- Superset UI (optional; run with: make demo-ui) --------
# IMPORTANT: change SECRET_KEY for any non-demo use.
SUPERSET_SECRET_KEY=dev-secret-key-change-later
SUPERSET_ADMIN_USER=admin
SUPERSET_ADMIN_PASS=admin
SUPERSET_ADMIN_EMAIL=admin@example.com

# DB connection used by Superset bootstrap script
DEMO_DB_NAME=demo
DEMO_DB_URI=postgresql+psycopg2://demo:demo@db:5432/demo

# -------- Hackathon Mode (Amazon Nova on Bedrock) --------
# Leave HACKATHON_MODE empty to use deterministic mock generator.
HACKATHON_MODE=amazon_nova
AWS_REGION=us-east-1
# Set to the Bedrock Nova model ID you are using
NOVA_MODEL_ID=

# Optional: if you use AWS profiles locally
# AWS_PROFILE=default
